repos:
- repo: local
  hooks:
  - id: dump-build-info
    name: Dump build info
    entry: bash -c "./buildinfo.bash > front/src/lib/build.json"
    language: system
    always_run: true
    pass_filenames: false